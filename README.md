# SillyTavern-FixHTML-release
# HTML Healer (SillyTavern Extension)

**HTML Healer** คือส่วนเสริมสำหรับ SillyTavern ที่ช่วยซ่อมแซมแท็ก HTML ที่แตก หาย หรือปิดไม่ครบ (Unclosed Tags) ในข้อความของ AI โดยอัตโนมัติ 

## ✨ ฟีเจอร์หลัก (Features)

* **🪄 Smart Quick Fix:** ปุ่มซ่อมด่วน! กดปุ๊บระบบจะตรวจเช็คข้อความ
    * หากเป็น **HTML ทั่วไป**: ระบบจะปิดแท็กที่หายไปให้ทันที และบันทึกให้อัตโนมัติ
    * หากเจอ **`<think>`**: ระบบจะ *ไม่* แก้ไขทันที แต่จะเปิดหน้าต่าง Editor ขึ้นมาให้ เพื่อป้องกันไม่ให้ข้อความความคิด (Thought) หายหรือปนกับเนื้อเรื่อง
* **🛡️ Whitelist Protection:** ระบบจะซ่อมเฉพาะแท็ก HTML มาตรฐาน (เช่น `div`, `span`, `style`, `p`) เท่านั้น
    * *ข้อดี:* หากคุณมีการใช้ Custom Tags แปลกๆ (เช่น `<my-custom-script>`) ระบบจะ **ข้าม** และไม่ไปยุ่งกับมัน ทำให้โค้ดเฉพาะทางของคุณไม่พัง
* **🧠 Split Editor:** หน้าต่างแก้ไขที่แยกส่วน "ความคิด" (Thinking) และ "เนื้อเรื่อง" (Story) ออกจากกัน ทำให้จัดการข้อความยาวๆ ได้ง่ายขึ้น

## 📦 วิธีติดตั้ง (Installation)

1.  ใช้ลิงก์ติดตั้งลงใน extension
    `(https://github.com/Ze4llaboizeng/SillyTavern-FixHTML-release/tree/main)`
2.  รีโหลดหน้าเว็บ SillyTavern
3.  เปิดเมนู Extensions จะเจอเมนู **HTML Healer**

## ⚠️ ขีดจำกัดของโปรแกรม (Limitations)

1.  **Custom Tags:** โปรแกรมจะไม่ซ่อมแท็กที่ไม่อยู่ในมาตรฐาน HTML5 (เช่นชื่อแท็กที่คุณตั้งขึ้นเอง) คุณต้องตรวจสอบความถูกต้องของแท็กเหล่านั้นด้วยตนเอง
2.  **ความซับซ้อนของโครงสร้าง:** แม้ระบบจะใช้ Stack Algorithm ในการจับคู่แท็ก แต่หากโครงสร้าง HTML ซับซ้อนมากหรือผิดเพี้ยนจนเกินไป (เช่น แท็กไขว้กันมั่ว) ผลลัพธ์การซ่อมอาจจะไม่สมบูรณ์ 100%
3.  **การแยกส่วน (Segmentation):** ในโหมด Editor การแยกบรรทัดแรกเริ่มจะดูจาก `\n` (การขึ้นบรรทัดใหม่) หากข้อความมาเป็นก้อนเดียวกันหมดโดยไม่เว้นบรรทัด ระบบอาจจะจับแยกส่วน `Start` ของเนื้อเรื่องได้ไม่แม่นยำในครั้งแรก (แต่คุณสามารถคลิกเลือกจุดตัดเองได้)

## ⚖️ ข้อตกลงและสิทธิ์การใช้งาน (License & Permissions)

**พัฒนาโดย:** [Zealllll] https://discord.com/users/211099311285600257

> ⛔ **คำเตือน:**
> 1.  **ไม่อนุญาต** ให้เผยแพร่, แจกจ่าย หรือนำไปลงใน Repository อื่นโดยทำการลบเครดิตหรือชื่อผู้สร้างออก (Do not remove the author's credit).
> 2.  หากต้องการนำ Code ไปดัดแปลงเพื่อเผยแพร่ต่อ (Modify for redistribution) **ต้องขออนุญาตจากผู้สร้าง (Zealllll) ก่อนเท่านั้น**
> 3.  อนุญาตให้ใช้งานส่วนตัว (Personal Use) และแก้ไขเพื่อใช้เองได้ตามสะดวก

---
*Created for the SillyTavern Community Thailand.*
